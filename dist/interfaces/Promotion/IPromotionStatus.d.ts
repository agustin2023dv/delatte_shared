/**
 * Contiene el estado y la fecha de creación de una promoción.
 * @interface IPromotionStatus
 */
export interface IPromotionStatus {
    /**
     * Estado actual de la promoción.
     * Puede ser "activa", "expirada" o "programada".
     * @example "activa"
     */
    estado: "activa" | "expirada" | "programada";
}
//# sourceMappingURL=IPromotionStatus.d.ts.map